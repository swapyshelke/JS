// object
const urlsObject = {
  development: "dev.example.com",
  production: "prod.example.com",
};

console.log(urlsObject.development);


// problems with object

// order is not fixed - with normal objects
// map maintaines the order of elements inserted

// size -> 